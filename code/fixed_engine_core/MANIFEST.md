# Fixed Engine Core Manifest

| Path | Role |
|---|---|
| `core/` | fixed q=8 engine source copied for implementation-lineage context |
| `analysis/` | fixed-engine analysis helpers retained for reproducibility context |
| `auxiliary/` | auxiliary fixed-engine probes retained for reproducibility context |
| `requirements.txt` | original local Python requirements |

The manuscript-supporting evidence is stored outside this directory under `data/`, `code/analysis/`, `figures/`, `supplements/`, and `metadata/`.
