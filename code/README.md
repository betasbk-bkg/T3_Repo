# Code Contents

`analysis/` contains package-relative scripts for the structural audit, fixed-engine equivalence check, clustered reanalysis, and table-specific recomputation.

`fixed_engine_core/` contains copied fixed-engine source for implementation-lineage and q=8 reproducibility context.

Running these scripts is optional for manuscript review. The repository package already includes the processed outputs used by the manuscript.
