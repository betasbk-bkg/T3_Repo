# Final Claim Boundary

Status: final claim lock after stream-separated CRN repair.

## Strong Claims Now Allowed

Allowed:

```text
The final stream-separated CRN audit supports a quantizer frequency-magnitude trade-off.
```

Allowed:

```text
Per-switch vote burden is larger under q4 than q16 with cluster-bootstrap uncertainty excluding zero.
```

Allowed:

```text
Positive counterfactual cost tails are larger under q4 than q16.
```

Allowed:

```text
A clustered interaction model supports non-additive vote/path obstruction channels.
```

## Claims Requiring Conservative Wording

Use conservative wording for:

- aggregate jump burden;
- path/vote co-occurrence;
- mean counterfactual event cost;
- sparse event-class means;
- square-specific reversals;
- q by path interaction.

## Claims Not Allowed

Not allowed:

- validation;
- predictive closure;
- complete global RMSE theorem;
- universal T3 benefit;
- coarse quantization is uniformly worse;
- every coincident event is harmful;
- event-class means alone prove non-additivity;
- observed values are theorem constants.

## Final Principle

Use:

```text
Quantizer refinement creates a frequency-magnitude trade-off, and vote/path obstruction channels are separable but non-additive.
```

Optional longer form:

```text
In delayed quantized collective-control simulations, smooth local relief cannot be closed globally by a single monotone switching-cost law because quantizer refinement changes event frequency and event magnitude in opposite directions, while vote and path events interact non-additively.
```

## Verdict

```text
CLAIM_BOUNDARY_FINALIZED_AFTER_STREAM_SEPARATED_CRN
```

